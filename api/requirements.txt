fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
# Runtime dependency of the wrapped engine (barotrauma.v2 imports numpy).
# Not pulled in transitively by FastAPI/uvicorn/pydantic, so it must be listed here.
numpy>=1.21.0
